import { useEffect, useState, type ReactNode } from 'react'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { Navigate, NavLink, Route, Routes, useParams } from 'react-router-dom'
import { AlertTriangle, Check, ChevronRight, Database, FileDown, KeyRound, LayoutDashboard, LogOut, Menu, Search, Settings, ShieldCheck, Users, X } from 'lucide-react'
import { getJson, getPage, getSession, login, logout, postDecision, postJson, type Principal, type Row } from './api/client'
import { campaignCtas, currentStateLabels, pageCount, pageLabel, roleHome } from './projections'
const s=(v:unknown,f='—')=>v==null||v===''?f:typeof v==='string'||typeof v==='number'?String(v):JSON.stringify(v), arr=(v:unknown):Row[]=>Array.isArray(v)?v.filter((x):x is Row=>!!x&&typeof x==='object'):[], vals=(v:unknown)=>Array.isArray(v)?v.map(String):[], pct=(v:unknown)=>Math.max(0,Math.min(100,Number(v)||0))
function App(){const q=useQuery({queryKey:['session'],queryFn:getSession,retry:false});return q.isLoading?<div className="loading-page">Loading EARE…</div>:q.isError?<Login/>:<Shell principal={q.data!}/>}
function Login(){const c=useQueryClient(),[u,setU]=useState(''),[p,setP]=useState(''),[e,setE]=useState('');const m=useMutation({mutationFn:()=>login(u,p),onSuccess:x=>c.setQueryData(['session'],x),onError:x=>setE(s(x,'Unable to sign in'))});return <div className="login-page"><form className="login-card" onSubmit={x=>{x.preventDefault();m.mutate()}}><h1>Sign in to EARE</h1><label>Username<input required value={u} onChange={x=>setU(x.target.value)}/></label><label>Password<input required type="password" value={p} onChange={x=>setP(x.target.value)}/></label>{e&&<p className="form-error">{e}</p>}<button className="button primary">Sign in</button></form></div>}
const links = [{to:"/",label:"Overview",icon:LayoutDashboard,roles:["ADMIN","OPERATOR"]},{to:"/reviews",label:"My reviews",icon:Check,roles:["ADMIN","OPERATOR","GROUP_OWNER"]},{to:"/identities",label:"Identities",icon:Users,roles:["ADMIN","OPERATOR"]},{to:"/accesses",label:"Access",icon:KeyRound,roles:["ADMIN","OPERATOR"]},{to:"/golden",label:"Golden Source",icon:ShieldCheck,roles:["ADMIN","OPERATOR"]},{to:"/campaigns",label:"Campaigns",icon:Check,roles:["ADMIN","OPERATOR"]},{to:"/findings",label:"Findings",icon:AlertTriangle,roles:["ADMIN","OPERATOR"]},{to:"/actions",label:"Actions",icon:Check,roles:["ADMIN","OPERATOR","BUSINESS_ADMIN"]},{to:"/reports",label:"Reports",icon:FileDown,roles:["ADMIN","OPERATOR"]},{to:"/sources",label:"Sources",icon:Database,roles:["ADMIN","OPERATOR"]},{to:"/system/users",label:"Users & permissions",icon:Users,roles:["ADMIN"]},{to:"/system/authentication",label:"Authentication",icon:Settings,roles:["ADMIN"]}]
function Shell({principal}:{principal:Principal}){const[c,setC]=useState(false),q=useQueryClient(),available=links.filter(x=>x.roles.includes(principal.role));return <div className="app-shell"><aside className={c?'sidebar open':'sidebar'}><div className="brand">EARE</div><nav>{available.map(({to,label,icon:I})=><NavLink key={to} to={to} end={to==='/' } onClick={()=>setC(false)} className={({isActive})=>isActive?'nav-link active':'nav-link'}><I size={17}/>{label}</NavLink>)}</nav></aside><div className="page"><header className="topbar"><button className="icon-button mobile-menu" onClick={()=>setC(!c)}><Menu/></button><span>Workspace › Access governance</span><button className="icon-button" onClick={async()=>{await logout();q.removeQueries({queryKey:['session']})}}><LogOut/></button></header><main><Routes><Route path="/" element={<Home/>}/><Route path="/reviews" element={<Reviews/>}/><Route path="/identities" element={<Identities/>}/><Route path="/accesses" element={<Accesses/>}/><Route path="/golden" element={<Golden/>}/><Route path="/campaigns" element={<Campaigns/>}/><Route path="/campaigns/:id" element={<CampaignDetail/>}/><Route path="/findings" element={<List path="findings" title="Findings"/>}/><Route path="/actions" element={<List path="remediation-actions" title="Actions"/>}/><Route path="/reports" element={<Reports/>}/><Route path="/sources" element={<Sources/>}/><Route path="/system/users" element={<UsersPage/>}/><Route path="/system/authentication" element={<Auth/>}/><Route path="*" element={<Navigate to={roleHome(principal.role)}/>}/></Routes></main></div></div>}
function Head({title,children}:{title:string,children?:ReactNode}){return <div className="page-header"><div><div className="eyebrow">EARE</div><h1>{title}</h1></div>{children}</div>};function Status({v}:{v:unknown}){return <span className="status">{s(v,'unknown').replaceAll('_',' ')}</span>};function Filter({v,onChange}:{v:string,onChange:(v:string)=>void}){return <div className="filterbar"><div className="search"><Search/><input placeholder="Search" value={v} onChange={e=>onChange(e.target.value)}/></div></div>};function debounce(v:string){const[d,setD]=useState(v);useEffect(()=>{const x=setTimeout(()=>setD(v),300);return()=>clearTimeout(x)},[v]);return d};
function Pager({total,limit,offset,setOffset,setLimit}:{total:number,limit:number,offset:number,setOffset:(n:number)=>void,setLimit:(n:number)=>void}){const pages=pageCount(total,limit),p=Math.floor(offset/limit)+1;return <div className="pagination"><span>{pageLabel(total,limit,offset)}</span><button className="button subtle" disabled={p<=1} onClick={()=>setOffset(offset-limit)}>Previous</button><span>Page {p} / {pages}</span><button className="button subtle" disabled={p>=pages} onClick={()=>setOffset(offset+limit)}>Next</button><select value={limit} onChange={e=>{setLimit(+e.target.value);setOffset(0)}}><option>25</option><option>50</option><option>100</option></select></div>}
function Table({cols,rows,q,onRow}:{cols:string[],rows:ReactNode[][],q?:any,onRow?:(i:number)=>void}){if(q?.isLoading)return <div className="empty">Loading…</div>;if(q?.isError)return <div className="empty">{s(q.error)} <button onClick={()=>q.refetch()}>Retry</button></div>;return <div className="table-wrap"><table><thead><tr>{cols.map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.length?rows.map((r,i)=><tr className={onRow?'clickable':''} onClick={()=>onRow?.(i)} key={i}>{r.map((x,j)=><td key={j}>{x}</td>)}</tr>):<tr><td colSpan={cols.length}>No records match this view.</td></tr>}</tbody></table></div>}
function Drawer({title,close,children}:{title:string,close:()=>void,children:ReactNode}){return <div className="drawer-backdrop" onClick={close}><aside className="drawer" onClick={e=>e.stopPropagation()}><div className="drawer-head"><h2>{title}</h2><button className="icon-button" onClick={close}><X/></button></div><div className="drawer-body">{children}</div></aside></div>}
function Home(){const q=useQuery({queryKey:['dashboard'],queryFn:()=>getJson('dashboard')});const m=(q.data?.metrics??{}) as Row;return <><Head title="Overview"/><div className="metrics">{[['Campaigns',m.campaigns],['Reviews remaining',m.pending_reviews],['Actions',m.remediation_actions],['Findings',m.findings]].map(x=><div className="metric" key={String(x[0])}><strong>{s(x[1],'0')}</strong><small>{String(x[0])}</small></div>)}</div></>}
function useList(path:string,extra:Row={}){const[search,setSearch]=useState(''),[offset,setOffset]=useState(0),[limit,setLimit]=useState(25),selected=debounce(search);const q=useQuery({queryKey:[path,selected,limit,offset,extra],queryFn:()=>getPage(path,{...extra,search:selected,limit,offset})});useEffect(()=>setOffset(0),[selected]);return{q,search,setSearch,offset,setOffset,limit,setLimit}}
function Identities(){const x=useList('identities'),[selected,setSelected]=useState<Row|null>(null);return <><Head title="Identities"/><Filter v={x.search} onChange={x.setSearch}/><Table cols={['Identity','Type','Source','Status','Accesses']} q={x.q} rows={(x.q.data?.items??[]).map(r=>[<button className="link-button" onClick={()=>setSelected(r)}>{s(r.identifier)}</button>,s(r.type),s(r.provider),<Status v={r.status}/>,s(r.access_count,'0')])}/><Pager total={x.q.data?.total??0} limit={x.limit} offset={x.offset} setOffset={x.setOffset} setLimit={x.setLimit}/>{selected&&<IdentityDrawer identity={selected} close={()=>setSelected(null)}/>}</>}
function IdentityDrawer({identity,close}:{identity:Row,close:()=>void}){const q=useQuery({queryKey:['identity-accesses',identity.id],queryFn:()=>getJson(`identities/${encodeURIComponent(s(identity.id,s(identity.identifier)))}/accesses`)}),[access,setAccess]=useState<Row|null>(null),rows: Row[]=[...arr(q.data?.accesses).map(r=>({...r,mode:'Direct'})),...arr(q.data?.effective_accesses).map(r=>({...r,mode:'Effective'}))];return <Drawer title={s(identity.identifier)} close={close}><h4>IDENTITY</h4><p>Status: {s(identity.status)}</p><h4>ACCESSES</h4><Table cols={['Access','Source','Permission','Mode','State']} q={q} rows={rows.map(r=>[<button className="link-button" onClick={()=>setAccess({provider:r.access_provider??r.provider,name:r.access_name})}>{s(r.access_name)}</button>,s(r.access_provider??r.provider),s(r.permission),s(r.mode),<Status v={r.classification??'observed'}/>])}/>{access&&<AccessDrawer access={access} close={()=>setAccess(null)}/>}</Drawer>}
function Accesses(){const x=useList('accesses'),[selected,setSelected]=useState<Row|null>(null);return <><Head title="Access"/><Filter v={x.search} onChange={x.setSearch}/><Table cols={['Access','Provider','Permission','Assignments']} q={x.q} rows={(x.q.data?.items??[]).map(r=>[<button className="link-button" onClick={()=>setSelected(r)}>{s(r.name)}</button>,s(r.provider),s(r.permission),s(r.assignment_count,'0')])}/><Pager total={x.q.data?.total??0} limit={x.limit} offset={x.offset} setOffset={x.setOffset} setLimit={x.setLimit}/>{selected&&<AccessDrawer access={selected} close={()=>setSelected(null)}/>}</>}
function AccessDrawer({access,close}:{access:Row,close:()=>void}){const[tab,setTab]=useState('overview'),provider=s(access.provider),name=s(access.name??access.access_name),q=useQuery({queryKey:['holders',provider,name],queryFn:()=>getJson(`accesses/${encodeURIComponent(provider)}/${encodeURIComponent(name)}/holders`)});return <Drawer title={name} close={close}><div className="tabs"><button onClick={()=>setTab('overview')}>Overview</button><button onClick={()=>setTab('holders')}>Holders</button></div>{tab==='overview'?<><p>Permission: {s(access.permission)}</p><p>Target: {s(access.target)}</p><p>Holder count: {arr(q.data?.holders).length+arr(q.data?.effective_holders).length}</p></>:<Table cols={['Identity','Direct / Effective','Why']} q={q} rows={[...arr(q.data?.holders).map(r=>({...r,mode:'Direct'})),...arr(q.data?.effective_holders).map(r=>({...r,mode:'Effective'}))].map((r: Row)=>[s(r.identity_identifier),s(r.mode),r.mode==='Direct'?'Direct grant':'Effective provenance'])}/>}</Drawer>}
function Reviews(){const x=useList('review-items'),[selected,setSelected]=useState<Row|null>(null);return <><Head title="My reviews"/><Filter v={x.search} onChange={x.setSearch}/><Table cols={['Identity','Access','State','Decision']} q={x.q} rows={(x.q.data?.items??[]).map(r=>[<button className="link-button" onClick={()=>setSelected(r)}>{s(r.identity_identifier)}</button>,s(r.access_name),<Status v={r.classification}/>,<Status v={r.decision??'pending'}/>])}/><Pager total={x.q.data?.total??0} limit={x.limit} offset={x.offset} setOffset={x.setOffset} setLimit={x.setLimit}/>{selected&&<ReviewDrawer item={selected} items={x.q.data?.items??[]} close={()=>setSelected(null)} next={setSelected}/>}</>}
function ReviewDrawer({item,items,close,next}:{item:Row,items:Row[],close:()=>void,next:(x:Row|null)=>void}) {
  const c=useQueryClient(), [reason,setReason]=useState(''), [pending,setPending]=useState<string|null>(null)
  const m=useMutation({mutationFn:(v:string)=>postDecision(s(item.id),v,v==='approve'?undefined:reason.trim()),onSuccess:async()=>{await c.invalidateQueries({queryKey:['review-items']});const i=items.findIndex(r=>r.id===item.id);next(items.slice(i+1).find(r=>!r.decision)||null);setPending(null);setReason('')},onError:()=>setReason('Decision failed')})
  const paths=arr(item.paths)
  return <Drawer title="Review item" close={close}><h4>WHY</h4>{item.direct?<p><strong>Direct grant</strong></p>:paths.length?<>{paths.map((p,i)=><p className="path" key={i}>{arr(p.steps??p.access_chain).map((z,j)=><span key={j}>{j?' → ':''}{s(z.name??z.identifier??z)}</span>)}</p>)}</>:<p>Provenance not available</p>}<h4>CURRENT STATE</h4>{currentStateLabels(Boolean(item.observed),Boolean(item.expected)).map(v=><p key={v}>{v}</p>)}<h4>CLASSIFICATION</h4><Status v={item.classification}/>{pending&&<div className="reason-form"><label>Reason *<textarea autoFocus value={reason} onChange={e=>setReason(e.target.value)}/></label><button onClick={()=>{setPending(null);setReason('')}}>Cancel</button><button disabled={!reason.trim()||m.isPending} onClick={()=>m.mutate(pending)}>Confirm {pending==='revoke'?'revoke':'N/A'}</button></div>}{reason==='Decision failed'&&<p className="form-error">Decision failed</p>}{!pending&&<div className="drawer-footer"><button onClick={()=>setPending('not_applicable')}>N/A</button><button onClick={()=>setPending('revoke')}>Revoke</button><button className="button primary" onClick={()=>m.mutate('approve')}>Approve</button></div>}</Drawer>
}
function List({path,title}:{path:string,title:string}){const x=useList(path),[selected,setSelected]=useState<Row|null>(null);return <><Head title={title}/><Filter v={x.search} onChange={x.setSearch}/><Table cols={['Identity','Access','Status']} q={x.q} rows={(x.q.data?.items??[]).map(r=>[<button className="link-button" onClick={()=>setSelected(r)}>{s(r.identity_identifier)}</button>,s(r.access_name),<Status v={r.status??r.classification}/>])}/><Pager total={x.q.data?.total??0} limit={x.limit} offset={x.offset} setOffset={x.setOffset} setLimit={x.setLimit}/>{selected&&<Drawer title={title.slice(0,-1)} close={()=>setSelected(null)}><pre>{JSON.stringify(selected,null,2)}</pre></Drawer>}</>}
function Campaigns(){const x=useList('campaigns'),snap=useQuery({queryKey:['snapshots'],queryFn:()=>getPage('snapshots',{limit:100})}),[form,setForm]=useState<Row>({name:'',snapshot_id:''}),[preview,setPreview]=useState<Row|null>(null),[allow,setAllow]=useState(false),previewM=useMutation({mutationFn:()=>postJson('campaigns/preview',{...form,scope:{type:'all'}}),onSuccess:setPreview}),create=useMutation({mutationFn:()=>postJson('campaigns',{...form,scope:{type:'all'},allow_unresolved_reviewers:allow}),onSuccess:d=>{window.location.href=`/campaigns/${s(d.id)}`}});return <><Head title="Campaigns"/><section className="panel"><h2>New campaign</h2><form onSubmit={e=>{e.preventDefault();previewM.mutate()}}><input required placeholder="Name" value={s(form.name,'')} onChange={e=>setForm({...form,name:e.target.value})}/><select required value={s(form.snapshot_id,'')} onChange={e=>setForm({...form,snapshot_id:e.target.value})}><option value="">Snapshot</option>{arr(snap.data?.items).map(r=><option key={s(r.id)} value={s(r.id)}>{s(r.created_at)}</option>)}</select><button>Preview campaign</button>{preview&&<><p>Review items {s(preview.total_review_items)} · Resolved {s(preview.resolved_reviewers)} · Unresolved {s(preview.unresolved_reviewers)}</p><label><input type="checkbox" checked={allow} onChange={e=>setAllow(e.target.checked)}/> Allow unresolved reviewers</label><button disabled={!allow&&Number(preview.unresolved_reviewers)>0} type="button" onClick={()=>create.mutate()}>Save as draft</button></>}</form></section><Filter v={x.search} onChange={x.setSearch}/><Table cols={['Campaign','Status','Progress','Pending']} q={x.q} rows={(x.q.data?.items??[]).map(r=>[<NavLink to={`/campaigns/${s(r.id)}`}>{s(r.name)}</NavLink>,<Status v={r.status}/>,`${pct(r.progress)}%`,s(r.pending,'0')])}/><Pager total={x.q.data?.total??0} limit={x.limit} offset={x.offset} setOffset={x.setOffset} setLimit={x.setLimit}/></>}
function CampaignDetail(){const{id=''}=useParams(),q=useQuery({queryKey:['campaign',id],queryFn:()=>getJson(`campaigns/${id}`)}),c=q.data?.campaign as Row|undefined,[tab,setTab]=useState('overview'),m=useMutation({mutationFn:(a:string)=>postJson(`campaigns/${id}/${a}`),onSuccess:()=>q.refetch()}),reviews=arr(q.data?.reviews),status=s(c?.status),pending=reviews.filter(r=>!r.decision).length,ctas=campaignCtas(status,pending);if(!c)return <div className="empty">{q.isLoading?'Loading…':'Campaign not found'}</div>;return <><Head title={s(c.name)}><NavLink to="/campaigns">Back</NavLink>{ctas.map(a=><button key={a} disabled={a==='close-disabled'} onClick={()=>m.mutate(a.replace('-disabled',''))}>{a}</button>)}</Head><div className="tabs"><button onClick={()=>setTab('overview')}>Overview</button><button onClick={()=>setTab('reviews')}>Reviews</button><button onClick={()=>setTab('findings')}>Findings</button></div>{tab==='overview'&&<div className="metrics"><div className="metric">Progress {pct(c.progress)}%</div><div className="metric">Pending {pending}</div></div>}{tab==='reviews'&&<Table cols={['Identity','Access','Decision']} rows={reviews.map(r=>[s(r.identity_identifier),s(r.access_name),<Status v={r.decision??'pending'}/>])}/>}<section className="panel"><h3>Generate report</h3><a href={`/api/reports/${id}/html`}>HTML</a> · <a href={`/api/reports/${id}/csv`}>CSV</a> · <a href={`/api/reports/${id}/json`}>JSON</a></section></>}
function Golden() {
  const q = useQuery({ queryKey: ['golden'], queryFn: () => getPage('golden-sources', { limit: 100 }) })
  const snap = useQuery({ queryKey: ['snap'], queryFn: () => getPage('snapshots', { limit: 100 }) })
  const [compare, setCompare] = useState<Row | null>(null)
  const [name, setName] = useState('Main baseline')
  const [sid, setSid] = useState('')
  const source = arr(q.data?.items)[0]
  const sourceName = s(source?.name, '')
  const baseline = useMutation({ mutationFn: () => postJson('golden-sources/baseline', { name, snapshot_id: sid }), onSuccess: () => q.refetch() })
  const compareMutation = useMutation({ mutationFn: () => getJson(`golden-sources/${sourceName}/compare`), onSuccess: setCompare })
  const confirm = useMutation({ mutationFn: () => postJson(`golden-sources/${sourceName}/confirm-version`, { observed_snapshot_id: compare?.observed_snapshot_id, active_golden_version_id: compare?.active_golden_version_id }), onSuccess: () => { setCompare(null); q.refetch() } })
  return <><Head title="Golden Source" />{!source ? <section className="panel"><h2>No expected baseline yet</h2>{arr(snap.data?.items).length ? <><select value={sid} onChange={e => setSid(e.target.value)}><option value="">Snapshot</option>{arr(snap.data?.items).map(r => <option key={s(r.id)} value={s(r.id)}>{s(r.created_at)}</option>)}</select><input value={name} onChange={e => setName(e.target.value)} /><button disabled={!sid} onClick={() => baseline.mutate()}>Create baseline</button></> : <p>No observed snapshot yet. Run a synchronization first.</p>}</section> : <><button onClick={() => compareMutation.mutate()}>Compare with latest observed</button>{compare && <section className="panel"><p>Added {arr(compare.changes).filter(r => r.status === 'added').length} · Removed {arr(compare.changes).filter(r => r.status === 'removed').length}</p><Table cols={['State', 'Identity', 'Access']} rows={arr(compare.changes).map(r => [<Status v={r.status} />, s(r.identity_identifier), s(r.access_name)])} /><button onClick={() => confirm.mutate()}>Confirm new version</button></section>}</>}</>
}
function Sources(){const q=useQuery({queryKey:['providers'],queryFn:()=>getPage('providers',{limit:100})}),[job,setJob]=useState(''),start=useMutation({mutationFn:(x:{p:string,a:string})=>postJson(`sources/${x.p}/${x.a}`),onSuccess:d=>setJob(s(d.id))}),jq=useQuery({queryKey:['job',job],queryFn:()=>getJson(`jobs/${job}`),enabled:!!job,refetchInterval:1500 as const});return <><Head title="Sources"/><div className="source-grid">{(q.data?.items??[]).map(r=><div className="source-card" key={s(r.name)}><h2>{s(r.display_name,s(r.name))}</h2><button onClick={()=>start.mutate({p:s(r.name),a:'preview'})}>Preview</button><button onClick={()=>start.mutate({p:s(r.name),a:'sync'})}>Run synchronization</button></div>)}</div>{job&&<section className="panel"><Status v={jq.data?.status}/><pre>{JSON.stringify(jq.data?.result??jq.data?.progress,null,2)}</pre></section>}</>}
function Reports(){const q=useQuery({queryKey:['reports'],queryFn:()=>getPage('campaigns',{limit:100})});return <><Head title="Reports"/>{(q.data?.items??[]).map(r=><div className="report-row" key={s(r.id)}><b>{s(r.name)}</b> <a href={`/api/reports/${s(r.id)}/html`}>HTML</a> <a href={`/api/reports/${s(r.id)}/csv`}>CSV</a> <a href={`/api/reports/${s(r.id)}/json`}>JSON</a></div>)}</>}
function UsersPage(){const q=useQuery({queryKey:['system'],queryFn:()=>getJson('system')});return <><Head title="Users & permissions"/><Table cols={['User','Role','Status']} q={q} rows={arr(q.data?.users).map(r=>[s(r.username),<Status v={r.role}/>,<Status v={r.enabled?'enabled':'disabled'}/>])}/></>};function Auth(){return <><Head title="Authentication"/><section className="panel"><h2>Local accounts</h2><Status v="ACTIVE"/><p>Username + password authentication is currently active.</p><h2>External SSO</h2><Status v="NOT_YET_ACTIVE"/><p>LDAP / OIDC / SAML provider configuration does not currently enable application sign-in.</p></section></>}
export default App
