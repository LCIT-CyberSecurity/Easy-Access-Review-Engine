# OpenLDAP

`exporters/openldap/export-openldap.sh` produit un LDIF portable. L'importeur supporte
`inetOrgPerson`, `groupOfNames`, `groupOfUniqueNames`, `posixGroup`, `member`, `uniqueMember` et
`memberUid`.
